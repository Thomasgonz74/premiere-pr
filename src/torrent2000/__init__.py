APP_NAME = "Torrent 2000"
# Single source of truth for update_checker.py's version comparison and the
# user-agent strings sent to trackers/RSS feeds/the GitHub Releases API --
# bump this (and pyproject.toml's version) when cutting a new release.
APP_VERSION = "1.1.1"
